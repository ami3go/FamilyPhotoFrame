// Entry point for scripts/verify-pure-logic.sh.
fun main() {
    runExifChecks()
    runSynologyChecks()
    runCertPinningChecks()
    runFrameStatsChecks()
    runSourcePolicyChecks()
    runSourceRuntimeSignatureChecks()
    runPlaybackQueueChecks()
    runFolderBalancedQueueChecks()
    runFolderBalancedPersistentChecks()
    runRememberedWebSecurityChecks()
    runSourcePoolPolicyChecks()
    runDiagnosticsJsonlChecks()
    runDiagnosticsV2Checks()
    runPrivacyChecks()
    runDiagnosticsRateAndHealthChecks()
    runDiagnosticsPagingAndBundleChecks()
    runCrashAndStallChecks()
    runRescanScheduleChecks()
    runWebDavChecks()
    runScanFilterChecks()
    runRecoveryPolicyChecks()
    runHostLifecycleGateChecks()
    runPlaybackMemoryPolicyChecks()
    runMemorySelfRecoveryChecks()
    runBrightnessTimelineChecks()
    runImageFormatSupportChecks()
    runScanMergePolicyChecks()
    runScanCompletionPolicyChecks()
    runStabilityHardeningChecks()
    println(if (failures == 0) "\nALL CHECKS PASSED" else "\n$failures CHECK(S) FAILED")
    if (failures != 0) kotlin.system.exitProcess(1)
}
